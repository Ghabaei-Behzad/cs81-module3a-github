# cs81-module3a-github
Functions: Git hub stores your files and their development history in a local repository. Whenever you save changes you have made, Git creates a commit. A commit is a snapshot of current files.

This repository contains a collection of JavaScript functions
journal.js
greet(name) : returns a greeting string.
isEven(num) : returns true if number is even, or false.
square(num) : returns square of a number.
celciusTofarenheit(c) : converts celcius to faren.
getLength(str) : returns number of characters in a string.
